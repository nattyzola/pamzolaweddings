#header
  #nav.desktop
    =link_to("Home", root_path(), :class => "#{'inactive' unless action_name == 'home'}")
    · 
    =link_to("About", about_path(), :class => "#{'inactive' unless action_name == 'about'}")
    · 
    =link_to("Services", services_path(), :class => "#{'inactive' unless action_name == 'services'}")
    · 
    =link_to("Inspiration", inspiration_path(), :class => "#{'inactive' unless action_name == 'inspiration'}")
    · 
    =link_to("Planning Tools", free_wedding_planning_tools_path(), :class => "#{'inactive' unless action_name == 'free_wedding_planning_tools'}")
    · 
    =link_to("Let's Talk", contact_path(), :class => "#{'inactive' unless action_name == 'contact'}") 
  #nav.mobile
    %p.page Pam Zola Weddings: #{action_name == 'free_wedding_planning_tools' ? 'Free Tools' : action_name.capitalize}
    %p.menu= link_to("Menu", '#', :class => "menu_link test")   
    %ul.mobile_menu
      %li= link_to("Home", root_path(), :class => 'inactive')
      %li= link_to("About", about_path(), :class => 'inactive')
      %li= link_to("Services", services_path(), :class => 'inactive')
      %li= link_to("Inspiration", inspiration_path(), :class => 'inactive')
      %li= link_to("Free Planning Tools", free_wedding_planning_tools_path(), :class => 'inactive')
      %li= link_to("Let's Talk", contact_path(), :class => 'inactive')
    <script> $('a.menu_link').click(function(){$('ul.mobile_menu').toggle();});</script>
